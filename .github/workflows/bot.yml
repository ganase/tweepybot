name: Run Tweepy Bot

on:
  schedule:
    - cron: '10 4 * * *'  # 毎日12:00 (JST) に実行 (UTC 03:00)
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tweepy Bot
      run: |
        python your_bot_script.py  # あなたのスクリプト名に置き換えてください
